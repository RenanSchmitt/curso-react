import React, { Fragment } from 'react';
import Header from './Header';

const Autores = () => {
    return(
        <Fragment>
            < Header/>
        </Fragment>

    );
}

export default Autores;